let determineSpeed = (way, time) => (way / time);
let determineSpeed2 = (way, time) => {
  return(way / time);
};
let determineSpeed3 = function(way, time) {
  return (way / time);
};
function determineSpeed4(way, time) {
  return (way / time);
}
console.log(determineSpeed(2, 2));
console.log(determineSpeed(8, 2));
console.log(determineSpeed(14, 2));
console.log(determineSpeed(6, 2));